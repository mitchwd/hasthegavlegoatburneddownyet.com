name: Linting

on: push

jobs:
  linting:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: jdx/mise-action@v4
      - run: bun install

      - run: bun run format --check
      - run: bun run lint
      - run: bun run tsc
